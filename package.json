{
  "name": "whyviral",
  "version": "1.0.0",
  "description": "WhyViral by Manish Tiwari — answers the only question that matters: why does this work? AI watches real videos from TikTok, Instagram, and Meta and pins the hooks, visuals and angles that earn distribution, with proof links. Local, private, file-based.",
  "type": "module",
  "scripts": {
    "start": "next start",
    "dev": "rm -rf .next && NODE_OPTIONS=--no-deprecation next dev --turbopack",
    "dev:all": "concurrently -n ui,mastra -c blue,magenta \"rm -rf .next && NODE_OPTIONS=--no-deprecation next dev --turbopack --port 3000\" \"npx --yes mastra@0.10.0 dev\"",
    "all": "concurrently -n ui,mastra -c blue,magenta \"rm -rf .next && NODE_OPTIONS=--no-deprecation next dev --turbopack --port 3000\" \"npx --yes mastra@0.10.0 dev\"",
    "dev:both": "concurrently -n ui,mastra -c blue,magenta \"rm -rf .next && NODE_OPTIONS=--no-deprecation next dev --turbopack --port 3000\" \"npx --yes mastra@0.10.0 dev\"",
    "all:full": "concurrently -n ui,mastra -c blue,magenta \"rm -rf .next && NODE_OPTIONS=--no-deprecation next dev --turbopack --port 3000\" \"npx --yes mastra@0.10.0 dev\"",
    "clean": "rm -rf .next .turbo node_modules/.cache",
    "build": "next build",
    "mastra": "mastra dev",
    "mastra:build": "mastra build",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "@mastra/core": "^0.10.0",
    "@mastra/libsql": "^0.10.0",
    "@mastra/memory": "^0.10.0",
    "@radix-ui/react-collapsible": "^1.1.3",
    "@radix-ui/react-dialog": "^1.1.6",
    "@radix-ui/react-separator": "^1.1.0",
    "@radix-ui/react-slot": "^1.1.2",
    "@radix-ui/react-tabs": "^1.1.3",
    "@radix-ui/react-toast": "^1.2.6",
    "apify-client": "^2.9.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "crawlee": "^3.12.0",
    "dotenv": "^16.4.0",
    "lucide-react": "^0.468.0",
    "next": "^15.3.0",
    "playwright": "^1.52.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-icons": "^5.7.0",
    "tailwind-merge": "^3.0.2",
    "tailwindcss-animate": "^1.0.7",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "autoprefixer": "^10.4.20",
    "concurrently": "^8.2.2",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.3.3",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.7.0"
  }
}
